{
  "name": "avianjets-rocket",
  "version": "1.0.0",
  "description": "Takes distance as input and calculates travel time with fibonacci loop",
  "main": "index.js",
  "bin": {
    "rocket": "./build/bin/rocket.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.22",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
